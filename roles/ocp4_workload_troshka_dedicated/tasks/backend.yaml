---
- name: Set cluster apps domain and requester identity
  ansible.builtin.set_fact:
    _cluster_apps_domain: "{{ sandbox_openshift_apps_domain }}"
    _troshka_route_host: "troshka-{{ troshka_namespace }}.{{ sandbox_openshift_apps_domain }}"
    _requester_identity: "{{ requester_email | default(sandboxes[0].annotations.owner_email | default('admin@troshka')) }}"

- name: Create backend ConfigMap
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: troshka-config
        namespace: "{{ troshka_namespace }}"
      data:
        config.yaml: |
          app:
            name: troshka
            port: 8200
            host: "0.0.0.0"
            log_level: info
            root_path: ""
            external_url: "https://{{ _troshka_route_host }}"
          auth:
            oauth_enabled: true
            jwt_algorithm: "HS256"
            admin_users: "{{ _requester_identity }}"
            allowed_users: "{{ _requester_identity }}"
            operator_users: ""
          s3:
            endpoint_url: "{{ troshka_s4_endpoint }}"
            bucket: "{{ troshka_s4_bucket }}"
            region: "us-east-1"
          defaults:
            run_timer_hours: 8
            lifetime_days: 30
            max_vms_per_project: 20
            max_projects_per_user: 10
            user_library_quota_gb: 500
          overcommit:
            cpu_ratio: 4.0
            ram_ratio: 1.5

- name: Deploy backend
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: troshka-backend
        namespace: "{{ troshka_namespace }}"
        labels:
          app.kubernetes.io/name: troshka-backend
      spec:
        replicas: 1
        selector:
          matchLabels:
            app.kubernetes.io/name: troshka-backend
        template:
          metadata:
            labels:
              app.kubernetes.io/name: troshka-backend
          spec:
            containers:
              - name: backend
                image: "{{ troshka_backend_image }}"
                ports:
                  - containerPort: 8200
                env:
                  - name: TROSHKA_DATABASE__URL
                    valueFrom:
                      secretKeyRef:
                        name: troshka-secrets
                        key: database-url
                  - name: TROSHKA_AUTH__JWT_SECRET
                    valueFrom:
                      secretKeyRef:
                        name: troshka-secrets
                        key: jwt-secret
                  - name: TROSHKA_AUTH__ENCRYPTION_KEY
                    valueFrom:
                      secretKeyRef:
                        name: troshka-secrets
                        key: encryption-key
                  - name: TROSHKA_S3__ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: troshka-secrets
                        key: s3-access-key
                  - name: TROSHKA_S3__SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: troshka-secrets
                        key: s3-secret-key
                volumeMounts:
                  - name: config
                    mountPath: /opt/app-root/src/config
                resources:
                  requests:
                    cpu: "{{ troshka_backend_cpu_request }}"
                  limits:
                    memory: "{{ troshka_backend_memory_limit }}"
            volumes:
              - name: config
                configMap:
                  name: troshka-config

- name: Create backend Service
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: troshka-backend
        namespace: "{{ troshka_namespace }}"
      spec:
        selector:
          app.kubernetes.io/name: troshka-backend
        ports:
          - port: 8200
            targetPort: 8200
